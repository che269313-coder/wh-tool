# 别名冲突待审核（2026-08-11）

来源：`node tools/apply-pdf-extraction.mjs` 合入 19 个阵营 PDF 提取结果后，
检查 `docs/rules/factions.js` 中同一别名 key 对应不同规范值的情况。

## 一、unitAliases 冲突

格式：阵营 | 别名 | 旧值 | 新值 | 建议

| 阵营 | 别名 | 旧值 | 新值 | 建议 |
| --- | --- | --- | --- | --- |
| chaos-space-marines（旧值归属 adepta-sororitas） | 犀牛装用车 | 修女会犀牛装甲车（修女会 PDF 提取，先合入） | 混沌犀牛战车（混沌星际战士 PDF 提取，后合入） | 暂不强行去重。两份 PDF 原文都把「犀牛装用车」写作默认装备/单位名，但各自阵营的规范名不同（修女会→修女会犀牛装甲车，混沌→混沌犀牛战车）。合入后两阵营的 `unitAliases` 各自保留了本阵营映射：adepta-sororitas 块为「犀牛装用车→修女会犀牛装甲车」，chaos-space-marines 块为「犀牛装用车→混沌犀牛战车」。查找方若按阵营过滤别名则无歧义；若全局查重需确认 app.js 的别名查找是否带阵营过滤后再决定是否删一方。 |

冲突计数：1 处（跨阵营 key 重复，值不同）。文件内同 key 重复、提取覆盖旧值的冲突：0 处。

## 二、附注：数量增强与 API 数据不一致项（非别名）

数量增强（PDF「Nx 武器名」写入 defaultEquipment 文本）与数据卡 `weapons[].count` 交叉核对
（53/54 项一致），以下 1 项不一致，待人工确认：

| 阵营 | 单位 | defaultEquipment 数量（来自 PDF） | weapons[].count（来自 API） | 说明 |
| --- | --- | --- | --- | --- |
| tau-empire | 危机阳铸战斗服 | 2x 融合爆破器 | 4 | PDF「2把融合炮」；API 认为每模型 4 件。3 模型单位 → PDF 语义下全队 6 件（2×3），API 语义下 12 件（4×3）。未强行统一，按 PDF 保留 2x。 |

另外以下单位 PDF 有数量但未改写（解析不出或需复核，保持原样）：
- 虎鲨战斗轰炸机 PDF 第 3 段被截断（「2门导」），仅前两件武器获得数量；
- 指挥官影阳的「1把箭弹发射器/1把轻型导弹仓」为 1 件，不改写；
- 逆戟鲸运输机/梭子鱼战机等规范名自带「×2」后缀的，不再叠加数量。
