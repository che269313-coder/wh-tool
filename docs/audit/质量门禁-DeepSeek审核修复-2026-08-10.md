# Quality Gate：DeepSeek 审核修复（2026-08-10）

## 愿景覆盖

| 用户要求 | 结果 |
| --- | --- |
| DeepSeek 审核只作线索，代码也按未审改动处理 | 已从原始 JSON、生成器和运行时独立复核，不直接采信报告结论 |
| 优先修 bug | 已修规则装配、无敌豁免择优、关键词、Unicode、数据生成、详情状态等已确认 bug |
| 优先实现阵营规则 | 19 个网站阵营由 0 条计算规则提升为 531 条安全可计算规则；欧克确认错误及达卡闪击已修 |
| 其他规则可暂缓 | 分遣队全面实现及非单轮战斗机制保持原文，没有伪标成计算支持 |
| 无法解决的矛盾交给用户审核 | 已写入 `docs/audit/DeepSeek审核复核与待裁定-2026-08-10.md` |

## 功能验收

| 项目 | 状态 | 证据 |
| --- | --- | --- |
| 阵营/单位效果公共契约 | 通过 | 19 个生成包全部通过 effect schema 校验 |
| 多选规则不同时启用 | 通过 | 千子仪式、星界军号令、恐虐祝福、黑暗契约等有专用选择控件 |
| 保守拒绝不可靠自动映射 | 通过 | 多选一、单颗骰重掷、动态规模规则回归测试 |
| 欧克重点规则 | 通过 | 8 个欧克专项回归场景 |
| 前端运行态 | 通过 | `C:\Users\che26\Desktop\wh-tool` 的 `http://127.0.0.1:8765/`；计算页单位选择、阵营/单位控件及武器详情正常渲染，console warning/error 为 0 |

## 设计稿与工件

- `designs/**/*.pen`：无匹配。
- 本次只有既有通用控件的数据接线与详情展开状态修复，没有新增页面布局。
- 仓库根目录媒体/设计工件：无。

## 本次验证证据

- `node --test tools/*.test.mjs`：37/37 通过，0 失败。
- `validate-40k-app.mjs`、`validate-architecture.mjs`、`validate-combat.mjs`、`validate-datasheets.mjs`、`validate-detachments.mjs`、`validate-rules.mjs`：6/6 通过。
- 对 57 个修改/新增 JS、MJS 文件执行 `node --check`：全部通过。
- `git diff --check`：通过。
- 项目没有 `package.json`，因此没有适用的 pnpm lint/build 命令；也不存在项目级 `check-hotfix-pattern.mjs` / `check-fallback-layers.mjs`。

